'use strict';

var projectsDirectives = angular.module('projectsDirectives', []);

projectsDirectives.directive('projectInfoBlock', function () {
  return {
    restrict: 'AE',
    templateUrl: "<%= asset_path('teamwork/project_info_block.html') %> "
  }
});
